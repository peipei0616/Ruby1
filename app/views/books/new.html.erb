<h1>新規投稿</h1>

<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

<div class="actions">
  <%= f.submit 'Create Book' %>
</div>
<% end %>